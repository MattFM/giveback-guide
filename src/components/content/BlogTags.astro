---
interface Props {
  tags: string[];
}

const { tags } = Astro.props;
---

{
  tags.length > 0 && (
    <p class="text-sm text-gray-400 dark:text-gray-400">
      {tags.join(" / ")}
    </p>
  )
}
